# Extra dependencies for the evaluation harness (not needed by the app itself).
# The core harness (eval/run_eval.py) only needs the app's existing deps.
# These are ONLY required for the optional `--ragas` pass.
ragas>=0.2,<0.3
datasets
langchain-openai
